<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JK Interactive Designs</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f6f9;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #2c3e50;
            color: white;
            padding: 20px;
            text-align: center;
            font-size: 2.5em;
            position: relative;
        }

        nav {
            display: flex;
            justify-content: center;
            background-color: #34495e;
            padding: 15px;
        }

        nav a {
            color: white;
            text-decoration: none;
            padding: 10px 20px;
            margin: 0 10px;
            font-size: 1.2em;
            transition: background-color 0.3s ease;
        }

        /* Pseudo-class hover for interactive navigation */
        nav a:hover {
            background-color: #e74c3c;
        }

        section {
            display: flex;
            justify-content: space-around;
            padding: 50px;
            text-align: center;
        }

        /* Pseudo-class nth-child to style different cards */
        .card:nth-child(odd) {
            background-color: #3498db;
        }

        .card:nth-child(even) {
            background-color: #e67e22;
        }

        .card {
            width: 250px;
            height: 300px;
            color: white;
            padding: 20px;
            border-radius: 10px;
            position: relative;
        }

        /* Pseudo-element before to add content decoration */
        .card::before {
            content: "★";
            font-size: 3em;
            position: absolute;
            top: 10px;
            right: 10px;
            color: rgba(255, 255, 255, 0.3);
        }

        input[type="text"] {
            padding: 10px;
            font-size: 1em;
            width: 200px;
            border: 2px solid #ccc;
            border-radius: 5px;
            margin: 20px 0;
        }

        /* Pseudo-class focus to style input box */
        input[type="text"]:focus {
            border-color: #2980b9;
            outline: none;
        }

        button {
            background-color: #27ae60;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 1.2em;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        /* Pseudo-class active to style button when clicked */
        button:active {
            background-color: #2ecc71;
            transform: translateY(2px);
        }

        footer {
            background-color: #2c3e50;
            color: white;
            padding: 20px;
            text-align: center;
            position: relative;
        }

        /* Pseudo-element after to add content decoration */
        footer::after {
            content: " - JK Interactive Designs - ";
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            background-color: #34495e;
            padding: 5px 10px;
            border-radius: 10px;
        }
    </style>
</head>
<body>
    <header>
        Welcome to JK Interactive Designs
    </header>

    <nav>
        <!-- Hover pseudo-class on navigation links -->
        <a href="#">Home</a>
        <a href="#">Services</a>
        <a href="#">Portfolio</a>
        <a href="#">Contact</a>
    </nav>

    <section>
        <!-- Pseudo-class nth-child for alternating card styles -->
        <div class="card">
            <h3>Web Design</h3>
            <p>High-quality web designs for all your business needs.</p>
        </div>
        <div class="card">
            <h3>App Development</h3>
            <p>Custom mobile apps with stunning UI and smooth UX.</p>
        </div>
        <div class="card">
            <h3>Graphic Design</h3>
            <p>Creative graphic designs that capture your audience.</p>
        </div>
    </section>

    <div class="container">
        <!-- Focus pseudo-class on input field -->
        <input type="text" placeholder="Enter your email" />

        <!-- Active pseudo-class on button -->
        <button>Subscribe</button>
    </div>

    <footer>
        &copy; 2024 JK Interactive Designs
    </footer>
</body>
</html>
